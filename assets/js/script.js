var cityCodes = [];
var cityNames = [];

function getRandomCities() {  
  for (var i = 0; i < 10; i++){
    i; 
  var num = Math.floor(Math.random() * 28065);
  cityCodes.push(num);
  }
  }

getRandomCities();

console.log(cityCodes);
