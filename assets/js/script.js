var cityCodes = [];
var cityNames = [];

function getRandomCities() {  
  for (var i = 0; i < 10; i++){
    i; 
  var num = Math.floor(Math.random() * 4503);
  cityCodes.push(num);
  }
  }

getRandomCities();
